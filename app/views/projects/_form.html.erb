<%= simple_form_for @project, remote: true do |f| %>
  <%= f.error_notification %>
  <%= f.input :name, label: "Project Name:" %>
  <br>
  <%= f.input :description %>
  <%= f.input :technologies_used, label: "Tech used", hint: "programming languages used" %>
  <%= f.button :submit %>

<% end %>
